import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "APEX OPTIONS | Professional Binary Options Trading",
  description:
    "Trade binary options with real-time charts, instant execution, and advanced risk management. Start with a $10,000 demo account.",
  keywords: ["binary options", "trading", "forex", "crypto", "options trading"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
